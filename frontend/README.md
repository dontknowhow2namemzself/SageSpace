# SageSpace — frontend

Next.js 14 (App Router) client for the SageSpace reading agent.

Setup, environment variables, and the full architecture overview live in
the project README: [../README.md](../README.md).

```bash
npm install
npm run dev   # http://localhost:3000 (expects the backend on :8000)
```
